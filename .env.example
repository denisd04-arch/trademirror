# Supabase (public client keys only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# AI Screenshot Parser (server-side only on Vercel)
# Option A: Vercel Connect (recommended on Vercel)
VERCEL_CONNECT_OPENAI_CONNECTOR=api.openai.com/trademirror

# Option B: Direct API key (local dev fallback)
OPENAI_API_KEY=

# Legacy flag (optional)
VITE_AI_PARSER_ENABLED=true
